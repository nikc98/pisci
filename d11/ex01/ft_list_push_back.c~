#include <stdlib.h>

#include "ft_list.h"

void	ft_list_push_back(t_list **begin_list, void *data)
{
    t_list *new;

    if (begin_list == NULL)
	return ;
    begin_list = ft_create_el(data);
    while (new->mext != 0)
	new = new->next;
    current->next = ft_create_elem(data);
}
